export const SearchBox = () => {
  return (
    <input
      type="text"
      placeholder="Search by title, tag or user..."
      className="w-full px-3 py-2 rounded-lg border border-[#dbe7ff] focus:outline-none focus:ring-2 focus:ring-[#2563eb] focus:border-transparent"
    />
  );
};

